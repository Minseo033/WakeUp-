<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WakeUp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WakeUp" parent="Base.Theme.WakeUp" />




    <style name="AlarmDayChip" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <!-- 가로는 글자 길이만큼 -->
        <item name="android:layout_width">wrap_content</item>
        <!-- 세로도 글자 기준으로 자동 -->
        <item name="android:layout_height">wrap_content</item>

        <!-- 최소 크기 없애기 (Material 기본값 48dp / 88dp 제거) -->
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>

        <!-- 안쪽 여백 -->
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>

        <!-- 글자 크기 & 간격 -->
        <item name="android:textSize">12sp</item>
        <item name="android:layout_marginEnd">4dp</item>



    </style>

    <style name="BottomNavTextActive" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">10sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="BottomNavTextInactive" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">10sp</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="SectionTitle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/alarm_label_color</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:textStyle">bold</item>
    </style>




</resources>